{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "esbuild resources/js/app.js --bundle --outfile=public/assets/app.js && esbuild resources/css/app.css --bundle --outfile=public/assets/app.css",
        "dev": "npm run build -- --watch"
    },
    "devDependencies": {
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "esbuild": "^0.24.0"
    }
}
